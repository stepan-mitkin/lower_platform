fun(element, left, top, width, height)

element.style.position = "absolute"
element.style.display = "inline-block"
element.style.left = left
element.style.top = top
element.style.width = width
element.style.height = height
